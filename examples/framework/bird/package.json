{
	"name": "l8b-bird",
	"version": "0.0.0",
	"private": true,
	"type": "module",
	"scripts": {
		"dev": "pnpm -w exec l8b dev",
		"build": "pnpm -w exec l8b build",
		"preview": "pnpm -w exec l8b preview"
	},
	"dependencies": {},
	"devDependencies": {
		"@l8b/cli": "workspace:*"
	}
}
