/**
 * Message templates for all diagnostics in L8B ecosystem
 *
 * Re-exports from messages/index.ts which combines all message categories
 */

export {
      formatMessage,
      getMessageTemplate,
      getSuggestions,
      MESSAGES,
} from "./messages/index";
